export const content = `
.x6-widget-group {
  position: relative;
}

.x6-widget-group-container {
  position: absolute;
  pointer-events: none;
}

.x6-widget-group-box {
  fill: transparent;
  stroke: #ccc;
  stroke-width: 1;
  stroke-dasharray: 5 5;
  pointer-events: all;
}

.x6-widget-group-label {
  position: absolute;
  background: #f0f0f0;
  padding: 4px 8px;
  border-radius: 3px;
  font-size: 12px;
  user-select: none;
}
`
